<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <h1>To Do List</h1>
  <hr />
  <h2>Tasks:</h2>
  @if (Model.Count != 0)
  {
    <ul>
      @foreach (var task in Model)
      {

        <li><a href='/tasks/@task.GetId()'>@task.GetDescription()</a>
          </li>

          <!-- <input type="checkbox"  checked value = 1>IS IT DONE????????????</input> -->
          if(@task.GetCompletion()==0)
          {
        <form action = "/tasks/@task.GetId()" method = "post">
          <input type="hidden" name="_method" value="PATCH">
          <input type="checkbox"  name = "checkbox" value = 1>NOT DONE YET!</input>
        <button type = "submit">Submit</button>
        </form>
        }

      if(@task.GetCompletion()==1)
      {

      <li>ITS DONE!</li>
      }




        <li>@task.GetCompletion()</li>
      }
    </ul>
  }




  <h4><a href="/tasks/new">Add new task</a></h4>
  <h4><a href="/">Return home</a></h4>
</body>
</html>
